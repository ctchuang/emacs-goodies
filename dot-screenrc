B# Reference: http://archerworks.blogspot.com/2010/05/linuxscreenbindkey.html

term screen

startup_message off

# Replace ctrl-A by ctrl-\ (All of the other keys are used by Emacs
escape ^\\

# Default title
shelltitle "-"

# Status line
hardstatus on
hardstatus alwayslastline " %-Lw%{= Bw}%n%f %t%{-}%+Lw %="
# hardstatus alwayslastline " %-Lw%{= Bw}%n%f %t%{-}%+Lw %=| %M %d %0c:%s "

# Refresh the display when exiting programs
altscreen on

# Enable non-blocking mode to better cope with flaky ssh connections.
defnonblock 5

vbell off

defutf8 on

# define a bigger scrollback, default is 100 lines. If it's too large (like
# 100k), it will drag down system performance.
defscrollback 5000

screen -t shark 10 cd ~/Code2/master.A
screen -t ryu 20
# screen -t nexus9 20
screen -t dut 50
screen -t ec 70
screen -t cpu 71
screen -t htop 99 htop
# This must be the last, or it will impact title of other sessions.
screen -t os 40 cros-enter-chroot
